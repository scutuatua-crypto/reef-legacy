{
  "name": "reef-legacy-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "nodemon --watch src --exec node src/index.js",
    "start": "node dist/index.js",
    "build": "echo \"No build step defined\"",
    "lint": "eslint . --ext .js",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.3.1",
    "pg": "^8.11.0",
    "ioredis": "^5.3.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22",
    "eslint": "^8.51.0"
  }
}
"scripts": {
  "test": "jest"
}
